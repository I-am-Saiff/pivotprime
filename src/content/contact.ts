/**
 * Contact page copy that is not in the spec.
 *
 * The standfirst read "Three fields, thirty seconds. We reply with a first read
 * on your bottleneck and a time for a 30-minute discovery call, with no pitch
 * deck." Two problems: it used "discovery call", which spec 2.2 line 226
 * instructs off the site, and it promised three fields when the message field is
 * optional. Rewritten to say the same thing without either.
 *
 * Ours, not the client's. PENDING-COPY 1k.
 */
export const CONTACT_PAGE = {
  standfirst:
    "Name and email is enough to start. We reply with a first read on your bottleneck and a time to talk it through, with no pitch deck.",
};
