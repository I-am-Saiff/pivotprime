{
  "_comment": "Raw hex colours that are correct as they stand. Each entry needs a reason. An entry here is a decision, not a snooze. The approved palette is five values defined in src/app/globals.css.",
  "allow": [
    {
      "file": "src/components/WhatsappButton.tsx",
      "hex": "#25D366",
      "reason": "Meta's mandated WhatsApp brand green. The WhatsApp brand guidelines require this exact value on the mark, so it is deliberately not a Pivot Prime token and must not be changed to --color-neon."
    },
    {
      "file": "src/app/diagnostic/page.tsx",
      "hex": "#f7f9f8",
      "reason": "The paper neutral declared by every approved mockup. It is the page surface behind DiagnosticApp, which is grandfathered, so this wrapper matches it. Remove this entry when the diagnostic component is tokenised."
    },
    {
      "file": "src/app/api/enquiry/route.ts",
      "hex": "#009f50",
      "reason": "Inline styles in a transactional email. Email clients do not support CSS custom properties, so a token cannot be used here and the literal hex is the only option. This is the mid green from the approved palette; keep it in step if the token changes."
    },
    {
      "file": "src/app/api/enquiry/route.ts",
      "hex": "#5e6f68",
      "reason": "Inline styles in a transactional email, same constraint as above. The muted grey used for quoted text throughout the approved mockups."
    },
    {
      "file": "src/app/api/diagnostic/route.ts",
      "hex": "#5e6f68",
      "reason": "Inline styles in a transactional email, the same constraint as the two entries for /api/enquiry above. Email clients do not support CSS custom properties, so a token cannot be used and the literal is the only option. The muted grey used for the received-at line, matching the enquiry notification."
    }
  ]
}
