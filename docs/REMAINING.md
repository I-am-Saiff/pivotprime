# Everything not built, and why

Internal. One place, as of 26 August 2026. The client-facing half of this is
`docs/FOR-IRAM-outstanding.md`; the reasoning and the code detail are in
`docs/PENDING-COPY.md`. This file is the index: what is not on the site, who owns
it, and what unblocks it.

**Nothing here is unrecorded work.** Every line names its entry.

---

## 1. The one thing that blocks launch

| | |
|---|---|
| **The five result figures** | The homepage results band renders five labels and five empty figures. Spec 3.3 ends "IRAM TO CONFIRM the five ranges above against the master table in Section 9", and section 1 says every result figure comes from that table and nowhere else. Four of the five on the old site are not in it. |
| **Owner** | Iram. One number per row. A range is a fine answer. |
| **Why it matters more than a blank space** | The results band is the most quotable thing on the site, and an answer engine looking at Pivot Prime today finds strong positioning and no performance number it can cite. `FOR-IRAM` 6d. |
| **Recorded** | `FOR-IRAM` 1, `PENDING-COPY` 1, gated in `audit-spec-copy` with all five entries including the 67%. |

The 67% is withheld from the band **although it is in the master table**, because
3.3's instruction covers all five. Publishing one figure beside four blanks reads
as four broken cards. It is published on the case study that earned it.

---

## 2. Waiting on Iram: material that does not exist yet

| What | Blocks | Entry |
|---|---|---|
| **The WhatsApp pre-fill message** | The header button opens WhatsApp with an empty message. Spec 2.2 says to point it "at WhatsApp with the homepage pre-fill" and never says what that message reads. We will not invent the first sentence a prospect sends her. | `FOR-IRAM` 6, `PENDING-COPY` 0.5 |
| **A share image** | The picture shown when a link is shared on LinkedIn or WhatsApp. 1200×630. Nothing in the project works as one and an unbranded default is worse than none. | `FOR-IRAM` 6 |
| **A background colour instruction** | Expected, never received. Her About redesign uses an off-white page background not on her swatch. Not applied. | `FOR-IRAM` 6 |
| **Four client logo descriptions** | Four logos in the client strip cannot be identified from their files, so their alt text and their clearance are both unresolved. | `FOR-IRAM` 6 |
| **The sixth metric card** | "Bespoke software and automation builds delivered" is written and ready with no number. Her document says not to launch it with a placeholder. A count of builds shipped turns it on. | `FOR-IRAM` 1.6 |
| **Client story figures and quotes** | See section 3 below — this is now a rule question, not missing material. | `PENDING-COPY` 1af |
| **The eighteen diagnostic commentary blocks** | Phase two. Nothing held up. | `FOR-IRAM` tail |
| **Screenshots of built systems** | For the technology page. Phase two. | `FOR-IRAM` tail |

**And one that is ours, not hers.** The logo was sent on 11 August, in Iram's
email titled "Pivot Prime Logo", five attachments and two PDFs. The client file
first said it was never sent, then said received and pending integration. **Both
were wrong. It has never been in the working folder and has never been opened
here.**

Status: **not seen**, and therefore not startable rather than not started. Five
attachments almost certainly means variants, and which variant belongs in the
header, the footer, the favicon and the share image needs the files open.
Saif is getting them across. `PENDING-COPY` 1j-a.

---

## 3. Waiting on Iram: decisions, not material

| Decision | The state of it | Entry |
|---|---|---|
| **The fees rule** | Her two fees designs publish $400,000, 20%, $320,000 and $80,000. Section 1 allows one price on the site and 3.10 bars a percentage or a formula. **The section is built to her layout with the figures replaced by wording.** The question is whether the rule moves, not which numbers to use. | `FOR-IRAM` 2, `PENDING-COPY` 1ae |
| **The credential figure** | Three sources now. 3.7 says over $120 million, 6.3 says $100 million, slide 21 says over $100 million. The homepage says $120m and the About card says $100m, **deliberately**, because the About card is her slide verbatim. The two pages disagree until she settles it. | `FOR-IRAM` 3, `PENDING-COPY` 1i |
| **The case study figures** | Two of her own files now agree on figures for the three anonymised studies, and none is in the section 9 table. **Not missing — barred.** One sentence from her turns them on. Same for Cinnacare and Scentmatic. | `PENDING-COPY` 1af |
| **Which clients may be named** | `pp-case-studies.html` states permission for Cinnacare and Scentmatic in her own words, and both are on the homepage on that authority. Written confirmation per client is still wanted. | `FOR-IRAM` 4, 6a |
| **Anonymity, on two pages at once** | Her `pp-case-studies.html` names two clients; her `pp-about-v2_2.html` says "Clients are anonymised". Both are on the site. The logo strip separately names seven clients. Three places must agree. | `PENDING-COPY` 1af, `FOR-IRAM` 6b |
| **The client quotes** | Her About design puts a quote under each new story, signed by a role and a company type. **None is published.** Each needs the quote plus confirmation the client agreed to it appearing. | `FOR-IRAM` 4 |
| **Team permission** | All four people are live with slide 21's titles and biographies. What is missing is one line from her per person confirming they are happy to appear. | `FOR-IRAM` 5 |
| **Two company names** | The logo strip says "Insurance Hub" and "Man Cave"; the logos read "InsuranceHub.ae" and "MANCAVE PROJECT". Not changed, because they are company names. | `FOR-IRAM` 6b |
| **Gallagher** | Removed from the client strip and now in the employer strip, because her biography names it as somewhere she worked. Goes back the same day if it is a delivery client. | `FOR-IRAM` 6c |
| **The employer strip heading** | "Experience inside global institutions" is her own line from her current site. Confirm or reword. | `FOR-IRAM` 6c |
| **Organization schema** | Can be added truthfully today, about an hour. Priced, not built, pending her go-ahead. Local-business listing needs a street address; founder profile waits on the credential figure. | `FOR-IRAM` 6e |
| **The P&L card routing line** | Her three new subtitles replaced the line saying which service delivers the outcome, on three of four cards. Does she want it kept? | `FOR-IRAM` 6f, `PENDING-COPY` 1z |
| **The fractional service card** | The menu says Fractional Leadership; the homepage card still says "Fractional COO, CFO and Chief of Staff", because that is a green block in 3.4. Our recommendation is in the client file. | `FOR-IRAM` 16, `PENDING-COPY` 1u |
| **The audience page headlines** | Sections 5.1–5.4 say keep the existing ones; her persona mockup carries four completely different ones. We built the document's. One line per page to switch. | `PENDING-COPY` 2.3 |
| **Spec 3.6** | Removed on her slide 6 comment, kept word for word. Her own document asked for it. | `FOR-IRAM` 12, `PENDING-COPY` 1w |
| **The slide 12 caption** | Her v2 deck says "REMOVE THE TEXT AT THE BOTTOM OF ANIMATION". She deleted the comment before v3 while keeping its neighbour. Withdrawn or resolved — only she knows. **Not actioned.** | `PENDING-COPY` 1af |
| **Em dashes** | Section 1 bans them; her own 22 August copy uses them throughout. We have converted nine. Three options put to her. | `FOR-IRAM` 9a, `PENDING-COPY` 1ag |
| **The colour names** | shell, mist, sand, bronze, linen. Ours, because her swatch labels only hexes. | `FOR-IRAM` 23 |
| **The privacy policy** | On the site but `noindex` until a UAE-qualified adviser has read it. | `FOR-IRAM` 7 |
| **Free-zone activity scope** | Only relevant to the technology page going live commercially. | `FOR-IRAM` 7 |
| **The six diagnostic section titles** | Discarded with the old layout. Hers if she wants them back as straplines. | `PENDING-COPY` 3.1a |
| **The neon in one mockup** | Her deep diagnostic mockup declares `#22c55e`; every other source says `#00d76d`, which is what the site uses. A note for whoever produced the mockups. | `PENDING-COPY` 4.2 |

---

## 4. Built differently from the document, and she has been told

None of these is outstanding work. They are listed so nothing is discovered later.

- **The fees section**: her layout, no figure but the audit floor. Her heading is
  now the section's H2, so spec 3.10's first block became the H3 over the
  traditional column. `PENDING-COPY` 1ae.
- **The About page**: rebuilt from slides 21 and 22, which replaces spec section 6
  outright. Gone with it: 6.1's four blocks, 6.2's four capabilities and their
  paragraphs, and 6.3's five-seat roles layer. All kept word for word.
  `PENDING-COPY` 1ab.
- **The case study split**: homepage carries the two named studies, /about carries
  all eight. Her own file settles the numbering and we had it inverted until
  26 August. `PENDING-COPY` 1y.
- **The patterns animation**: a reveal, not a typing effect, because a typing
  effect puts nothing on the page for a crawler. `PENDING-COPY` 3.3.
- **The case studies are stacked, not a carousel.** 3.8 says keep the carousel and
  then calls it ugly; 11.3 records that it clips. We took the second half.
  `PENDING-COPY` 3.5.
- **Two homepage sections moved to /about**, being outside the 3.1–3.12 order.
  Vetoable without rewriting a word. `PENDING-COPY` 2.4.
- **The /for-smes pricing paragraph** removed so three cards match.
  `PENDING-COPY` 1aa.
- **How We Work is off the site**, on her slide 17 comment, taken at face value.
  **Closed, not pending**: 404, out of the menu, out of the sitemap, every
  sentence preserved, one flag restores it, and `check-content` fails if the
  route stops 404ing. `PENDING-COPY` 1x, `FOR-IRAM` 14.
- **Eighteen lines of connective copy are ours**, grouped by page for her.
  `PENDING-COPY` 1f.

---

## 5. Phase two, deliberately out of stage one

| | |
|---|---|
| **The diagnostic** | Flagged off. The instrument at `/diagnostic` is the deep 42-statement version; the split into a public 12-question one and an unlisted deep one is later-phase, so `/diagnostic/deep` 404s on its own account. `PENDING-COPY` 0.1–0.3. |
| **Question order** | **Reopened, and the instruction has not been read here.** It reaches the record as Saif's account of a 12 August email nobody on this side has opened. "Random order, not grouped" already leaves two questions open: across all six domains or within a screen, and whether the six-step progress indicator survives. **Nothing to be built on the paraphrase.** The email itself unblocks it. `PENDING-COPY` 1j-a, 3.1. |
| **Two gated sentences** | Spec 3.11's homepage close standfirst, and the diagnostic destination on the second CTA, which points at `/contact` with her own "Talk to us" wording. Both return automatically. `PENDING-COPY` 0.4, 0.6. |
| **Joint-constraint reporting** | Where two constraints combine the diagnostic reports them singly, which is narrower than the document. `PENDING-COPY` 3.2. |
| **The report template** | 11 headings in her `report-template.html` are on no page, because none of it is built. |
| **SEO and AEO beyond the agreed set** | Everything not in `FOR-IRAM` 6e was agreed as a later phase. |

---

## 6. Our work items, nothing owed by her

| | |
|---|---|
| **Four service page components are client components without needing to be** | `Service1ClarityAudit`, `Service2FractionalLeadership`, `Service3BuildPlace`, `Service5MarketEntry`. Their content is fully in the served HTML — measured on all nine service and audience pages against the fully-expanded DOM, zero words missing. A hydration cost and a shape that invites the served-HTML defect back, not a live defect. `PENDING-COPY` 1j. |
| **Three palette leftovers** | `--color-primary-dark`, `--color-dark`, `--color-light` in `globals.css`. The first is the hover state on every primary button, so dropping it changes every button on the site. Not done unasked. `PENDING-COPY` 4.4. |
| **The logo** | **Cannot start: the file has never been here.** Sent 11 August, not yet across. Header, footer, favicon and share image change together. |
| **`check:dropped` still reports one** | "Real problems. Real results." from `pp-about-v2_2.html`, superseded by her later `pp-case-studies.html` wording. Explained in `KNOWN_ABSENT`, not a gap. |

---

## 7. What the checks say, on the build this file was written against

```
overflow-check   clean, 45 page loads across 360, 768 and 1440
content-check    clean, 153 spec assertions with JavaScript off
link-check       15 pages, 424 internal links, every one lands
behaviour-check  clean, 22 interaction assertions
reverse-audit    clean, 67 traced, 18 awaiting the client
audit:copy       0 of 198 spec copy blocks missing
dropped-copy     nothing of hers missing without a reason
lint             eslint, copy-lint and palette-lint clean
tests            31 passing
```

`npm run check` runs the two reporting steps at the end now. Findings never fail
it; a run that could not cover the site still does.
